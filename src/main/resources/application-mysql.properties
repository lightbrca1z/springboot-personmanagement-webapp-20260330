spring.datasource.url=jdbc:mysql://${SPRING_DATASOURCE_HOST}:${SPRING_DATASOURCE_PORT}/${SPRING_DATASOURCE_DATABASE}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL:true}

spring.flyway.enabled=${SPRING_FLYWAY_ENABLED:false}

# spring.datasource.url=jdbc:mysql://localhost:8889/persondb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Tokyo
# spring.datasource.username=root
# spring.datasource.password=Keeper2020
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# 既存DBに people だけある場合は、Hibernate の ddl-auto=update で app_users / people.user_id が追加されます。
# 既存行に user_id が付かないと MySQL で制約エラーになるため、新規スキーマか、手動で user_id を埋めてから運用してください。
#
# email 列・created_at / updated_at の手動 SQL は次を参照（通常は起動時 Flyway で補完。Hibernate 単体では列リネームしません）。
# classpath:sql/mysql/people_email_and_timestamps.sql
